#include <bits/stdc++.h>
using namespace std;
void solve(){
    int n,k;
    cin >> n >> k;
    vector<int> lab(n);
    iota(lab.begin(), lab.end(), 1);
    if(k%2==0){
        
    }
}

int main(){
    int t; cin >> t;
    while(t--){
        solve();
    }
}